# 安全

## 先说边界

Termspace 里所有终端和应用本身跑在**同一个用户身份**下。底层是 tmux，
任何已经能在你机器上执行命令的进程，都可以直接向那些会话发送按键 ——
绕开画布上的连线、弹窗、开关，一个都不经过。

所以画布上的「连线即授权」和弹窗确认，**是产品护栏，不是安全边界**。
它防的是 agent 拿错账号、连错库、把活派到你没打算派的地方，以及你自己手滑。
**它不防一个已经拿到你 shell 的攻击者。**

完整的安全模型（做到了什么、每条挡住的具体场景、更新链逐环的「保证 / 不保证」）
写在 <https://termspace.app/security/>。

## 报告一个问题

**请不要开公开 issue。** 走 GitHub 的私密漏洞报告：
仓库的 **Security → Report a vulnerability**。只有仓库维护者能看到。

会认真处理的，除了常规漏洞，还包括这两类：

- **文档与实现不符** —— 安全页、README、代码注释里任何一句描述与代码
  实际行为不一致。这一页和那一页的价值全在于它们是准确的，
  所以「你写的和你做的不是一回事」本身就是一个有效报告。
- **护栏可被绕过的具体路径** —— 即使最终落在上面那条 UID 边界之内。
  知道一条具体的绕法，比知道「理论上可以绕」有用得多。

报告时请尽量给：复现步骤、你用的版本（设置 → 更新里能看到）、
以及它实际造成了什么（读到了什么、执行了什么、绕过了哪个闸）。

## 不在范围内

- 需要攻击者**已经能在目标机器上执行命令**才成立的路径 —— 见上面的边界声明。
  这类欢迎报告并会记进文档，但不会当成漏洞修，因为它本来就在承诺之外
- 依赖库的已知漏洞：请直接报给上游。若它在 Termspace 里被以危险方式使用，
  那部分算 Termspace 的问题
- 缺少某项加固措施但没有具体利用路径的（"没有启用 X"）

## 支持范围

alpha 阶段，只维护最新版本。修复随下一个版本发布，
客户端会自己发现更新，但**装不装由用户点** —— 不会自动安装。

## 凭证

如果你怀疑自己的账号凭证泄漏了：Termspace 的凭证用系统钥匙串加密存在本机，
不出网、无服务端。轮换对应 provider 的凭证即可，不需要联系我们 ——
我们没有你的任何数据。
